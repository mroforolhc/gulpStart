<% _.forEach( icons, function( icon ){ %>
$icon-<%= icon.id %>-width = <%= icon.width %>;
$icon-<%= icon.id %>-height = <%= icon.height %>;<% }); %>

<% _.forEach( icons, function( icon ){ %>.svg-<%= icon.id %> {
  width: $icon-<%= icon.id %>-width;
height: $icon-<%= icon.id %>-height;
}
<%= icon.style %>
<% }); %>
